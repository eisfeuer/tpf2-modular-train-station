local PlatformModuleUtils = require('motras_platformmoduleutils')

function data()
    return {
        cost = {
            price = 18000,
        },
        category = {
            categories = { "passenger", },
        },
        description = {
            name = _("platform"),
		    description = _("350mm"),
            icon = "ui/construction/station/rail/motras/platform_350_era_c.tga"
        }, 
        availability = {
            yearFrom = 0,
            yearTo = 0
        },
        type = "motras_platform",
        order = {
            value = 10350,
        },
        metadata = {
        },
        updateFn = function(result, transform, tag, slotId, addModelFn, params)
            local platform = result.motras:register(slotId, {platformHeight = 0.35})
            PlatformModuleUtils.makePlatformModule(platform, result, transform, tag, slotId, addModelFn, params)
        end,
        getModelsFn = function()
            return { }
        end
    }
end