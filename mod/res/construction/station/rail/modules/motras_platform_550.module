local PlatformModuleUtils = require('motras_platformmoduleutils')

function data()
    return {
        cost = {
            price = 18000,
        },
        category = {
            categories = { "passenger", },
        },
        description = {
            name = _("platform_550"),
		    description = _("platform_550_desc"),
            icon = "ui/construction/station/rail/modular_station/track.tga"
        }, 
        availability = {
            yearFrom = 0,
            yearTo = 0
        },
        type = "motras_platform",
        order = {
            value = 1,
        },
        metadata = {
        },
        updateFn = function(result, transform, tag, slotId, addModelFn, params)
            print('katze')
            local platform = result.motras:register(slotId)
            print(require('inspect')(platform))
            platform:handle(function (moduleResult)
                PlatformModuleUtils.makePlatform(
                    platform,
                    result.models,
                    'station/rail/motras/platform_rep.mdl'
                )
                print(require('inspect')(result.models))
            end)
        end,
        getModelsFn = function()
            return { }
        end
    }
end